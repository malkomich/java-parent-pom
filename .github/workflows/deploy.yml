name: Deploy snapshot

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source code
        uses: actions/checkout@v2

      - name: Set up Java for publishing snapshot to Maven Central Repository
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
          server-id: ossrh
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD

      - name: Configuring Maven cache
        uses: actions/cache@v1
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Deploy to the Maven Central Repository
        run: mvn -B -ntp -Possrh clean deploy
        env:
          MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}

      - name: Configure Git
        run: |
          git config user.email "noreply@github.com"
          git config user.name "Github"

      - name: Create Snapshot tag
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q)
          git tag -f -a v$VERSION -m "Snapshot version ${VERSION}"
          git push -f --tags
